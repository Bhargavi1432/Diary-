import type { Configuration } from "@azure/msal-browser";

/* =========================
   MSAL CONFIGURATION
========================= */
export const msalConfig: Configuration = {
  auth: {
    clientId: "YOUR_CLIENT_ID", // from Azure
    authority: "https://login.microsoftonline.com/YOUR_TENANT_ID",
    redirectUri: "http://localhost:5173", // must match Azure EXACTLY
  },

  cache: {
    cacheLocation: "localStorage", // keeps user logged in
    storeAuthStateInCookie: false,
  },
};

/* =========================
   LOGIN REQUEST (REQUIRED)
========================= */
export const loginRequest = {
  scopes: ["User.Read"], // permission to read user profile
};